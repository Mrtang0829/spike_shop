<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.tz.spike_shop.mapper.SpikeGoodsMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.tz.spike_shop.pojo.SpikeGoods">
        <id column="id" property="id" />
        <result column="goods_id" property="goodsId" />
        <result column="spike_price" property="spikePrice" />
        <result column="store_count" property="storeCount" />
        <result column="start_date" property="startDate" />
        <result column="end_date" property="endDate" />
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        id, goods_id, spike_price, store_count, start_date, end_date
    </sql>

</mapper>
